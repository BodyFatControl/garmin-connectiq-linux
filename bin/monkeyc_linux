#!/bin/bash

MB_HOME="$( cd "$( dirname "$0" )" && pwd )"
DEVELOPER_KEY="$MB_HOME"/developer_key.der

java -classpath "$MB_HOME"/monkeybrains.jar com.garmin.monkeybrains.Monkeybrains -a "$MB_HOME"/api.db -u "$MB_HOME"/devices.xml -y "$DEVELOPER_KEY" "$@"
